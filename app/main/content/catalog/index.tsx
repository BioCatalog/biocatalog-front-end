import { CatalogProps, RecordProps } from "@/interfaces"

interface CatalogDetailProps {
    catalog: CatalogProps
    records: RecordProps
}

export default function Catalog({ catalog, records }: CatalogDetailProps) {

    return (
        <>
        </>
    );
}